This is a test document for Neo4j integration testing.

It contains multiple paragraphs to test document storage and retrieval.

The document should be properly stored with embeddings generated by OpenAI's text-embedding-3-small model.
