# Weave CLI Environment Variables Example
# Copy this file to .env and update with your actual values

# =============================================================================
# REQUIRED VALUES AND SECRETS
# =============================================================================

# Vector Database Configuration
# Choose your database type: weaviate-cloud, weaviate-local, mock
VECTOR_DB_TYPE="weaviate-cloud"

# =============================================================================
# WEAVIATE CLOUD CONFIGURATION
# =============================================================================
# Required for weaviate-cloud database type

# Your Weaviate Cloud URL (get this from your Weaviate Cloud dashboard)
WEAVIATE_URL="https://your-cluster-id.c0.us-west3.gcp.weaviate.cloud"

# Your Weaviate Cloud API key (get this from your Weaviate Cloud dashboard)
WEAVIATE_API_KEY="your-weaviate-api-key-here"

# OpenAI API key (required for embeddings in weaviate-cloud)
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-your-openai-api-key-here"

# =============================================================================
# OPTIONAL VALUES AND SECRETS
# =============================================================================

# Collection Names (optional - defaults will be used if not set)
VECTOR_DB_TEXT_COLLECTION_NAME="WeaveDocs"
VECTOR_DB_IMAGE_COLLECTION_NAME="WeaveImages"

# Test Collection Names (optional - defaults will be used if not set)
WEAVIATE_TEXT_COLLECTION_TEST="WeaveDocs_test"
WEAVIATE_IMAGE_COLLECTION_TEST="WeaveImages_test"

# =============================================================================
# CONFIGURATION NOTES
# =============================================================================
# 
# 1. For weaviate-cloud:
#    - Requires: WEAVIATE_URL, WEAVIATE_API_KEY, OPENAI_API_KEY
#    - Get credentials from: https://console.weaviate.cloud/
#    - OpenAI API key needed for text embeddings
#
# 2. For weaviate-local:
#    - Requires: Local Weaviate instance running on localhost:8080
#    - No API keys needed for local development
#    - Set VECTOR_DB_TYPE="weaviate-local"
#
# 3. For mock (testing):
#    - No external dependencies required
#    - Set VECTOR_DB_TYPE="mock"
#    - Perfect for development and testing
#
# 4. Security:
#    - Never commit .env file to version control
#    - Keep API keys secure and rotate them regularly
#    - Use different keys for development and production